import "./globals.css";

export const metadata = {
  title: "Glidex Quality — ISO Certification Body",
  description:
    "Glidex Quality provides internationally recognized ISO management system certifications.",
};

export default function RootLayout({ children }) {
  return children;
}
